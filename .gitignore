# deps
node_modules/

# local env
*.local

# aider
.aider*

# eslint cache
.eslintcache

# build output
dist/

# parity workflow runtime state (the engine .claude/workflows/parity.js is tracked; its run state is not)
.claude/parity/