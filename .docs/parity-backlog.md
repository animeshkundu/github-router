# Parity Backlog

**Run timestamp:** 2026-06-01T05:24:51Z
**Claude Code version:** 2.1.159
**Mode:** fix

This is the rerunnable parity ledger. CONFIRMED GAPS are actionable proxy work; DEFERRED items are parked with a reason; the COVERED / INTENTIONAL / NOT-PROXY-RELEVANT table records prior verdicts so we don't re-investigate them on the next run. Idempotent runtime state lives in `.claude/parity/state.json` (gitignored).

## CONFIRMED GAPS

| Feature | Severity | Status | PR/Branch | One-line design |
|---|---|---|---|---|
| Strip Anthropic `speed:"fast"` top-level body field on `/v1/messages` + `/v1/messages/count_tokens` (fast-mode body-field half; `fast-mode-2026-02-01` header already wired) | Important | fix-ready (gates green) | `wf_aaf9e4b2-451-63` (worktree off master; not committed/pushed) | strip — top-level `body.speed` property-access delete in BOTH `stripAnthropicOnlyFields` copies, gated by `"speed"` raw-body fast-path; mirrors the `budget` strip. Header keeps forwarding intent; only the unenforceable latency hint is dropped (Copilot 400s on unknown top-level fields). |
| GA structured outputs: schema-preservation prompt-injection for `output_config.format` (nested GA shape) | Important | confirmed | — | strip + gate(prompt-injection). Wire strip already works field-agnostically. Fix is schema-CAPTURE only: read schema/type from GA nested `oc.format.schema` / `oc.format.type` in addition to pre-GA flat `oc.schema` / `oc.type`, with legacy-flat precedence, so `appendStructuredOutputInstruction` fires for GA requests. No new strip/gate/upstream-shape change. |
| `web_fetch` hosted-tool type (beta `web-fetch-2025-09-10` / GA `2026-02-17`; slugs `web_fetch_20250910`, `web_fetch_20260209`) | Important | confirmed | — | GATE via fail-fast 400 (mirrors `mcp_servers` precedent). Match ONLY on hosted-tool `type` slug (`/^web_fetch_\d{8}$/`), NEVER on tool name (client-side custom `web_fetch` must still 200). Gate must run BEFORE side-effecting prefulfillment so a mixed web_search+web_fetch request rejects without first running an MCP search. Cover both `/v1/messages` and `/v1/chat/completions`. |
| Fast mode (Opus 4.8 fast-mode setting; `CLAUDE_CODE_OPUS_4_6_FAST_MODE_OVERRIDE`) | Suggestion | confirmed (doc/probe hygiene only) | — | passthrough (already present). `fast-mode-` prefix already forwarded under leverage, stripped under stealth. 2x-price/2.5x-speed tier is an Anthropic billing/infra semantic with no Copilot backend → upstream-unsupportable. Only actionable work: probe + matrix + ledger + CLAUDE.md note. The env override is client-side; proxy does NOT read it. |
| Cache diagnostics beta (`cache-diagnosis-2026-04-07` header + `diagnostics.previous_message_id` body + `diagnostics.cache_miss_reason` response) — graceful degradation | Suggestion | confirmed | — | strip(body) + passthrough(header/response). (1) Strip unknown top-level `body.diagnostics` in BOTH `stripAnthropicOnlyFields` copies (Copilot 400s on unknown top-level fields). (2) Header stays dropped via existing allowlist-only `filterBetaHeader` (no Copilot backend; dropping is correct end state). No response-path change; no synthetic `cache_miss_reason` ever generated. |

## DEFERRED queue

_(none this run)_

| Feature | Reason class | Note |
|---|---|---|

## COVERED / INTENTIONAL / NOT-PROXY-RELEVANT

| Feature | Status | Ledger ref |
|---|---|---|
| Dynamic workflows (harness) — `/workflows` rerunnable orchestration driving background agents | not-proxy-relevant | No ledger line names `/workflows` (postdates listed docs). Closest analog: `docs/auth-isolation.md:30` classifies `claude --bg` supervisor state (daemon/, daemon.log) as ISOLATED — proxy stays out of local background orchestration. |
| `/workflows` slash command (view/manage dynamic workflows) | not-proxy-relevant | No src/ or docs/ refs; `workflow` only matches GitHub Actions CI. Client-side harness/UI; spawned background agents reach proxy as ordinary `/v1/messages` passthrough. Fan-out = more concurrent requests, already covered by stream-lifecycle handling. |
| Auto mode (`CLAUDE_CODE_ENABLE_AUTO_MODE`) — model auto-selects effort/routing on Bedrock/Vertex/Foundry | not-proxy-relevant | Same cloud-gateway-gate exclusion class as `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY` (intentionally NOT auto-enabled, `docs/claude-env-injection.md`); USE_BEDROCK/VERTEX/FOUNDRY stripped at `src/lib/launch.ts:43-45`. |
| `/effort` slash command + xhigh reasoning level (thinking→effort translation) | fully-supported | `docs/copilot-compat-matrix.md:68` (`output_config:{effort}` ✅ 200 preserved, proxy-internal/translateThinking) |
| `claude --bg` / `--exec` (background/detached sessions resumable via `/resume`) | not-proxy-relevant | `docs/auth-isolation.md:30` — jobs/, daemon/, daemon.log classified ISOLATED (mirror would cross credential domains). |
| `--fallback-model` (cli flag) | passthrough-covered | `src/lib/utils.ts:150` resolveModel entry; `:186-280` resolution cascade; `src/lib/error.ts:48-60` forwardError; `docs/default-models.md:22` explicit `-m`/`--model` respected as-is; `:12,18` server-side fallback chains are distinct concept. |
| MessageDisplay hook (settings.json hook transforming assistant text before display) | not-proxy-relevant | No proxy ref to MessageDisplay / settings.json hook events. Fires client-side; never alters wire protocol. Existing user hook survives via MIRRORED settings.json (`src/lib/paths.ts:157-159`). |
| SessionStart hook `reloadSkills` + `sessionTitle` output fields | not-proxy-relevant | No src/ or docs/ refs. Hooks executed by client; output fields parsed locally by Claude Code; never appear in forwarded request bodies. settings.json MIRRORED so hooks still fire. |
| `disallowed-tools` frontmatter (skills/slash commands) | not-proxy-relevant | `src/lib/codex-mcp-config.ts:340-349` buildAgentMd emits only name/description; `docs/peer-mcp-design.md:15` no `tools:` field; `tests/codex-mcp-config.test.ts:416-422`. No proxy path handles disallowed-tools. |
| `/reload-skills` (endpoint) — rescans skill dirs without restart | not-proxy-relevant | No skills API surface (no route under src/routes/); default-MIRRORED config policy (`src/lib/paths.ts:209-210`) covers `~/.claude/skills` implicitly. |
| `allowAllClaudeAiMcps` setting (loads claude.ai cloud MCP connectors) | not-proxy-relevant | `docs/unsupported-features.md:13` — inline `mcp_servers` fail-fast 400 governs only request-time consequence; the setting is a pure client↔claude.ai-cloud control. |
| `pluginSuggestionMarketplaces` setting | not-proxy-relevant | `docs/auth-isolation.md:3` — settings.json MIRRORED; proxy does not interpret managed plugin settings. |
| `worktree.bgIsolation` setting (CLI background-session worktree behavior) | not-proxy-relevant | Zero repo refs. Pure local-harness setting; never crosses API boundary. Proxy's own `src/lib/worker-agent/worktree.ts` is the unrelated `worker_implement({worktree:true})` feature. Survives via MIRRORED settings.json. |
| EnterWorktree mid-session switching (tool-type) | not-proxy-relevant | Local-harness git op under project `.claude/worktrees/`; nothing crosses API. Proxy's PID-prefix sweep is namespaced to `<repoRoot>/.git/worker-worktrees/` via strict regex (`worktree.ts:73-74`); zero collision with CLI worktrees. |
| `claude agents --json` / settings-driven agent + `--agent` override + 2.1.142 cli flags | passthrough-covered | `src/claude.ts:278` captures trailing args via citty `_`; `src/lib/launch.ts:203` forwards verbatim; `src/claude.ts:426-430`. Settings-driven `agent` field via MIRRORED settings.json. `--model`/`--effort` resolve through normal path. |
| `CLAUDE_CODE_PACKAGE_MANAGER_AUTO_UPDATE` | passthrough-covered | Not in injected experimentalEnables (`src/lib/server-setup.ts:445-451`); not in STRIPPED_PARENT_ENV_KEYS (`src/lib/launch.ts:28-60`); pure local-binary self-upgrade feature, no Copilot-API surface. |
| `CLAUDE_CODE_POWERSHELL_RESPECT_EXECUTION_POLICY` | not-proxy-relevant | No code ref. Not in auto-enabled set; not auth/cloud key so passes through to spawned claude untouched; local shell behavior only. |
| Plugins auto-load from `.claude/skills/` + `claude plugin init` + `defaultEnabled:false` manifests | not-proxy-relevant | `docs/copilot-compat-matrix.md:108` (skills- API beta non-goal); `docs/auth-isolation.md:32` (plugins/ via MIRRORED policy). |
| `/chrome` browser selection (endpoint) — v2.1.154 TUI pick among connected browsers for Claude-in-Chrome | not-proxy-relevant | CHANGELOG v2.1.154 confirms client-side TUI selection, no new endpoint/header/body. Proxy `--browse` is a separate from-scratch MV3 extension (`src/lib/browser-mcp/`), not Anthropic's Claude-in-Chrome. |
| Lean system prompt default (setting) — lean system prompt for newer Opus | passthrough-covered | `src/routes/messages/handler.ts:93-100,676-677,868-876` — body.system transforms are content-uniform/model-agnostic; proxy never sizes by model. `docs/copilot-compat-matrix.md:56`. Lean-vs-full decided client-side. |
| Mid-conversation system messages (`role:"system"` at non-first positions) | passthrough-covered | `src/routes/messages/handler.ts:680-691` sanitizeCacheControl role-agnostic; `:53-66` extractUserQuery user-only; `:307` forwards untouched; `src/services/copilot/create-messages.ts:62` verbatim; `src/lib/sanitize-anthropic-body.ts` no system-position logic. |
| Refusal categories in `stop_details` (response body-field) on `/v1/messages` | passthrough-covered | `src/routes/messages/handler.ts:442-478` non-streaming verbatim c.json; `:428-436` streaming relay; `src/lib/stream-relay.ts` byte-level relay, zero stop_reason/stop_details refs. |
| advisor tool (header `advisor-tool-2026-03-01`) | fully-supported | `docs/unsupported-features.md:11` — Phase I shipped, proxy injects `__anthropic_advisor`, runs advisor model server-side (gpt-5.5 xhigh), emits translated server_tool_use{advisor} + advisor_tool_result; `docs/beta-headers.md:14`. |
| extended-output 300k beta (`output-300k-2026-03-24`) raising max_tokens to 300k on Batches API | intentionally-unsupported | `docs/copilot-compat-matrix.md:109` — `output-128k-` ❌ 400 (not in allowlist); same allowlist mechanism governs output-300k-. |
| `inference_geo` / data residency (body-field) | passthrough-covered | `docs/copilot-compat-matrix.md:59` (class-level: preserve unknown fields unless documented; no specific row/probe). |
| compaction API (body-field) — `context_management.edits[].type=compact_20260112` (beta `compact-2026-01-12`) | fully-supported | `docs/copilot-compat-matrix.md:61` (compact_20260112 row) + `:90` (compact- prefix, verified live). |
| automatic caching (body-field) — single cache_control breakpoint auto-advances | passthrough-covered | `docs/copilot-compat-matrix.md:47` — `cache_control:{type,ttl?}` ✅ 200, standard cache-control forwarded untouched. |
| Programmatic tool calling (`code_execution_20260120` tool-type + `allowed_callers`; GA 2026-02-17) | intentionally-unsupported | `docs/copilot-compat-matrix.md:108` (code-execution- ❌ 400) + `:38` (code_execution_20250825 ❌ 400, not in Copilot allowlist). Same non-goal class as Files API / Skills. |
| context editing thinking-block clearing (`clear_thinking_20251015`, body-field `context_management.edits[]`) | passthrough-covered | `docs/copilot-compat-matrix.md:61` (clear_thinking_20251015 in upstream allowlist) + `:81` (context-management- prefix always-allowed). Accepted passthrough, not a non-goal. |
| `model_context_window_exceeded` stop reason (new stop_reason value) | passthrough-covered | No stop_reason allowlist on path. `src/lib/stream-relay.ts:120-122` verbatim enqueue; `src/routes/messages/handler.ts:478` verbatim c.json. Feature string appears nowhere in codebase. |
| effort defaults high on Opus 4.8 (thinking→effort translation default) | passthrough-covered | Server-side backend default (high == omitted). (1) Explicit-effort passthrough: `PROXY_OWNED_FIELDS={"effort"}` (`handler.ts:752-800`). (2) Legacy translation fallback already defaults budget→8000→`high` (`handler.ts:578-587`), client effort still wins (`:650`). Opus 4.8 resolution wired (`src/lib/utils.ts:212-216`). No Opus-4.8-specific code needed. |
| `mcp_servers` MCP connector (body field) + MCP tunnels research preview (2026-05-19) | intentionally-unsupported | `docs/unsupported-features.md:13` (non-empty fail-fast 400, points to local stdio MCP; empty `[]` passes through); `docs/copilot-compat-matrix.md:68` + `:60`. |

## Ship results (this run)

- **Strip `speed:"fast"` body field** — `fix-ready` on branch `wf_aaf9e4b2-451-63` (isolated worktree off master; not committed, not pushed). Full 7-file footprint mirroring the proven `budget` strip: `src/routes/messages/handler.ts`, `src/routes/messages/count-tokens-handler.ts`, `scripts/probe-copilot-compat.sh` (2 registry rows + `speed_fast_stripped` / `speed_fast_count_tokens_stripped` functions, both sending `anthropic-beta: fast-mode-2026-02-01`), `docs/copilot-compat-matrix.md`, `docs/beta-headers.md`, `CLAUDE.md`, `tests/messages-handler.test.ts` (4 tests: top-level strip; body stripped while fast-mode header still forwards; count_tokens path; nested-speed-in-tool-input_schema preserved + verbatim byte-for-byte passthrough). ALL GATES GREEN: `lint:all` clean, `typecheck` clean, `bun test` 1017 pass / 0 fail / 23 skip across 70 files, `probe:copilot --strict` 26 passed / 0 failed (live enterprise Copilot proxy, both new probes confirmed PASS individually and in suite, no existing probe regressed). codex_reviewer self diff-review: "Clean review — no findings." Only the 7 intended files modified; dist/ gitignored and untracked. No commit, no push, no PR (mode=fix). No AI attribution anywhere. Worktree left changed for inspection.
